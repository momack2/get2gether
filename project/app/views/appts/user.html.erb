<div class="main">
<% if (@id and @curr) %>
  <h1>Get2gethrs for: <%= @curr.first_name %> <%= @curr.last_name %></h3>

   <% if @appts.empty? %>
  		<p>Sorry, <%= @curr.first_name %> doesn't have any Get2gethers right now...</p>

  <% else %>
  <div>
  	<ul class="view form">
  		<form action="/appts/book/<%= @curr.id %>" method="get">
		  <% @appts.each do |appt| %>
		  	<% if !appt.booked %>
		  		<li class="phone">
		  			<input type="radio" name="appt_id" value="<%= appt.id %>" />    <b><%= appt.event_name %>:</b> <%= appt.other %> <%= appt.start_time %> - <%= appt.end_time %>
		  			<small><%=appt.description %></small>
		  		</li>
			<% end %>
		  <% end %>
		  <li><input type="submit" class="btn btn-large btn-success" value="Plan it!" /></li>
  		</form>
  	</ul>
  </div>
  <% end %>
  
<% end %>
</div>
    
